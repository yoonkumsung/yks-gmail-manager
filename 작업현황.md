# 작업현황 — 재개용 단일 문서 (READ THIS FIRST)

> 이 문서 하나만 읽으면 현재 상태와 다음 할 일을 파악해 재개할 수 있다.
> 최종 갱신: 2026-06-14

---

## 한 줄 상태

**분석·전수조사·계획 완료. 개발(코드 수정)은 아직 0줄 시작 안 함.** 다음 단계 = `보완계획.md` Phase 0부터 개발 착수.

---

## 지금까지 한 것 (분석 단계, 완료)

2026-06-01 리포트의 품질 문제 16개를 출발점으로, 파이프라인 전 코드 + 실제 데이터 + 전 뉴스레터를 전수 조사해 **근본원인 18개(RC-A~R)** 를 규명하고 보완계획을 확정했다.

**산출 문서 (전부 `docs/` 또는 루트):**
| 문서 | 내용 |
|---|---|
| **`보완계획.md`** ★ | 개발 계획 Phase 0~5, LLM vs 룰베이스 분담, 뉴스레터별 개선표. **개발 시 이거 보고 진행** |
| `docs/분석_근본원인.md` | 근본원인 RC-A~R 18개 상세 + 증거 + LLM/룰 분류 |
| `docs/품질대조_20260501/_종합.md` | 라벨별 콘텐츠 대조 종합 |
| `docs/품질대조_20260501/_라이브리포트_매핑.md` | 사용자 지적 16개 → RC 매핑 |
| `docs/품질대조_20260501/_커버리지_뉴스레터전수.md` | 123 발신자/19 라벨 전수 커버리지 + 죽은 라벨 |
| `docs/품질대조_20260501/_뉴스레터별_종합.md` | 미검증 88개 전수 대조 종합 |
| `docs/품질대조_20260501/{라벨}.md`, `뉴스레터별_chunkN.md`, `*_전건.md` | 라벨/뉴스레터별 상세 대조 로그 (영구) |

**검증 범위(전수):** 구조(에이전트24·SKILL101·카탈로그101) + 콘텐츠(123 발신자·19 라벨·800+ 아이템) + 라벨(죽은 라벨까지 규명). 더 팔 분석 영역 없음.

---

## 근본원인 18개 요약 (상세는 분석_근본원인.md)

| RC | 문제 | 계층 |
|---|---|---|
| A | 출처가 LLM 자유생성 → 불안정(SCMP 5분리, 에이스퀘어 3철자) | RULE |
| B | link 기반 중복제거 부재(티저+원문, cross-메일) | RULE |
| C | 라벨 가드 침묵 실패(gmail_labels 없으면 no-op) | RULE |
| D | 무의미 한줄 정크(바이라인/헤더; NYT "By저자" 파서결함) | RULE+LLM |
| E | 번역 누락(소셜포럼 파손, 해외 22개 번역규칙 없음, 인명 음역오류) | LLM |
| F | 렌더모드(목록/카드/브리핑) 미분류 | RULE/CONFIG |
| G | 리포트 인덱스 404(reports/reports 경로 중복) | RULE |
| H | 코드경로별 enrich 불일치 | RULE |
| I | 라벨 에이전트 8개 파손(경제·기타·라이프·마케팅·소셜포럼·스포츠·시사·인문학) | 복구 |
| J | 혼종 CJK(일본어 한자 누출) | RULE+LLM |
| K | 카탈로그 메타 stale(item_count_avg 부정확, 중복 엔트리) | CONFIG |
| L | validate_skills 얕음(파손·번역결핍 못 잡음) | 검증 |
| M | 정보 누락(대형 브리핑 헤드라인 폐기, 중국_시사 17건) | RULE+LLM |
| N | 크롤링 오염(footer/지난호/홍보 무차별 → 정크·날짜오염) | RULE |
| O | 할루시네이션(김범석→김봉진, $57B→57억) | LLM+RULE |
| P | html_to_text 본문 전소실(KOTRA·녹색소비자·경기RISE) | RULE |
| Q | 라벨 fetch silent 0(NYT 246/14d인데 백필 0건 무경보) | RULE+알림 |
| R | NYT류 멀티에디션 라벨내 대량 중복 | RULE |

**P0 핵심(16개 중 12개 해소):** A(출처 룰베이스화) · B(link dedup) · I(파손복구). 추가 P0급: M(누락)·N(크롤오염)·Q(fetch 0).

---

## 다음 할 일 (개발, 미착수)

`보완계획.md` 순서대로:
1. **Phase 0** — 레포정리(완료: 루트 junk 제거됨) + enrich 공용함수화
2. **Phase 1** — 출처 룰베이스화 / 파손 에이전트 8개 복구 / link dedup / 병합규칙 완화
3. **Phase 1.5** — 누락방지 / 크롤오염차단 / 본문소실복구 / 할루억제 / fetch silent-0 방지
4. **Phase 2~5** — 정크필터·번역·라우팅·렌더모드·뉴스레터별(SCMP SKILL 등)·문서정합

각 Phase 후 `npm test` + (Phase 1·2 후) Haiku 전수 대조로 검증.

---

## 개발용 데이터 위치 (gitignore됨, 디스크에만)

| 경로 | 용도 |
|---|---|
| `output/backfill/20260501/` | **핵심 테스트 픽스처** — 6/1 라벨별 raw/clean/items/merged. 수정 효과 대조 기준 |
| `output/audit_missing_20260601/` | NYT 재수집·재추출 데이터 + 감사 스크립트(`prep_missing.js`·`make_worklist.js`·`prep_nyt_econ.js`·`worklist_88.json`) |
| `output/backfill/20260502~30/` | 30일 발신자 census용 참고 데이터 |

> 전부 `.gitignore`의 `output/`에 포함돼 커밋되지 않음. 개발 검증 시 재사용.

---

## 죽은 라벨 (개발 전 확인 필요)

- **글로벌_경제·스포츠**: 21일+ 수신 0건. 비활성 또는 구독상태 점검(보완계획 F4-0).
