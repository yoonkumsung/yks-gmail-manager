# Credentials (민감 정보)
config/credentials/
config/user_profile.json
*.json.bak

# Output 디렉토리
output/
gmail-digest.json

# Claude Code 로컬 설정 (moai 템플릿은 머신마다 재생성 → 로컬 전용)
# 단, 커스텀 스킬은 git으로 동기화 (다른 머신에서 git clone 시 따라옴)
.claude/*
!.claude/skills/
.claude/skills/*
!.claude/skills/digest-quality-audit/

# Windows 예약어 파일
nul

# Node.js
node_modules/
package-lock.json
pnpm-lock.yaml
yarn.lock

# 환경 변수
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# 로그
*.log
logs/

# 임시 파일
*.tmp
*.temp

# 런타임 생성 (SKILL 건강도 추적)
config/skill_health.json

# 테스트 커버리지 리포트 (nyc + c8)
coverage/
.nyc_output/
